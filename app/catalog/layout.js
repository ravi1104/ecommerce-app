export const metadata = {
    title: "Catalog",
    description: "E-commerece app",
  };
  
export default function Layout({children}) {
    return (
        <div>
            {children}
        </div>
    );
}